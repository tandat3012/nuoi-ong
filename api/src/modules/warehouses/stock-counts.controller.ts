import { Body, Controller, Get, Param, Patch, Post, Query } from '@nestjs/common';
import { parseOptionalEnum, parsePagination, requireUuid } from '../../common/query-params';
import { stockCountStatus } from '../../db/schema';
import { CurrentAuth } from '../auth/current-auth.decorator';
import { CreateStockCountDto, UpdateStockCountDto } from './dto/stock-count.dto';
import { StockCountsService } from './stock-counts.service';
@Controller('api/v1/stock-counts') export class StockCountsController { constructor(private readonly service: StockCountsService) {} @Get() list(@CurrentAuth() a: { clerkUserId: string }, @Query('farmId') farmId?: string, @Query('page') page?: string, @Query('pageSize') pageSize?: string, @Query('status') status?: string) { return this.service.list({ clerkUserId: a.clerkUserId, farmId: requireUuid(farmId, 'farmId'), ...parsePagination(page, pageSize), status: parseOptionalEnum(status, stockCountStatus.enumValues, 'status') }); } @Post() async create(@CurrentAuth() a: { clerkUserId: string }, @Query('farmId') farmId: string | undefined, @Body() b: CreateStockCountDto) { return { data: await this.service.create(requireUuid(farmId, 'farmId'), a.clerkUserId, b) }; } @Get(':id') async get(@CurrentAuth() a: { clerkUserId: string }, @Param('id') id: string, @Query('farmId') farmId?: string) { return { data: await this.service.get(requireUuid(id, 'id'), requireUuid(farmId, 'farmId'), a.clerkUserId) }; } @Patch(':id') async update(@CurrentAuth() a: { clerkUserId: string }, @Param('id') id: string, @Query('farmId') farmId: string | undefined, @Body() b: UpdateStockCountDto) { return { data: await this.service.update(requireUuid(id, 'id'), requireUuid(farmId, 'farmId'), a.clerkUserId, b) }; } @Post(':id/confirm') async confirm(@CurrentAuth() a: { clerkUserId: string }, @Param('id') id: string, @Query('farmId') farmId?: string) { return { data: await this.service.confirm(requireUuid(id, 'id'), requireUuid(farmId, 'farmId'), a.clerkUserId) }; } @Post(':id/cancel') async cancel(@CurrentAuth() a: { clerkUserId: string }, @Param('id') id: string, @Query('farmId') farmId?: string) { return { data: await this.service.cancel(requireUuid(id, 'id'), requireUuid(farmId, 'farmId'), a.clerkUserId) }; } }
